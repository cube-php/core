<?php

namespace App\Models{subNamespace};

use Cube\Http\Model;

class {className} extends Model
{
    protected static $schema = '{name}';

    protected static $fields = array(
        'id',
        'created_at',
        'updated_at'
    );
}